# dlt: BigQuery + filesystem (GCS) destinations
dlt[bigquery]>=1.0.0
dlt[filesystem]>=1.0.0
dlt[gs]>=1.0.0

# Data & APIs
pandas>=2.0.0
requests>=2.28.0
beautifulsoup4>=4.12.0
pyarrow>=14.0.0
datasets>=2.14.0
kaggle>=1.5.0

# Validation
pydantic>=2.0.0

# Optional: load .env for run_ingestion and scripts
python-dotenv>=1.0.0

# Dashboard (BigQuery explorer UI)
streamlit>=1.28.0
google-cloud-bigquery>=3.14.0

# Skills extraction (LLM)
google-generativeai>=0.8.0
