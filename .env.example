# Required for ingestion: GCP project and GCS bucket (from Terraform: terraform -chdir=terraform output -raw project_id / gcs_bucket_name)
GOOGLE_CLOUD_PROJECT=your-gcp-project-id
GCS_BUCKET=your-project-job-lakehouse-raw

# Required for Kaggle pipelines only: get from https://www.kaggle.com/settings → API → Create New Token (downloads kaggle.json)
KAGGLE_USERNAME=your_kaggle_username
# Use KAGGLE_KEY or KAGGLE_API_TOKEN (both work)
KAGGLE_KEY=your_kaggle_api_key
# KAGGLE_API_TOKEN=your_kaggle_api_key
