# Dev / CI (install: pip install -r requirements-dev.txt)
-r requirements.txt
pytest>=8.0.0
ruff>=0.8.0
# dbt-core 1.10.9+ needs protobuf>=6; google-generativeai pins google-ai-generativelanguage 0.6.15
# which requires protobuf<6. Cap dbt until the Gemini stack allows protobuf 6+.
dbt-core>=1.10.0,<1.10.9
dbt-bigquery>=1.10.0,<1.11
